// Must stay in sync with `basePath` in next.config.ts. Next.js only
// auto-prefixes assets referenced through next/image, next/link and
// next/script; anything else (like the raw HTML embed below) needs this
// prefix added by hand.
export const basePath = "/ente-arquitectos";
